import { observer } from "mobx-react-lite"
import { Stack, Button, Typography, Box, Divider, Paper, Tooltip, Badge, Avatar, Grid } from '@mui/material';


const InventoryManager = observer(({ resourceHandler, actionHandler }) => {

    return (
        <Stack direction="column">
            <Stack >

            </Stack>
        </Stack>
    )

})

export const EquipmentManager = observer(({ resourceHandler, actionHandler }) => {

    return (
        <Stack direction="column">
            <Grid >

            </Grid>
        </Stack>
    )

})


